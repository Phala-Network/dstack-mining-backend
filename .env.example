# ===== DStack Backend Configuration =====

# Docker image version
# Use 'main' for development or 'v0.2.0' for production
DSTACK_BACKEND_IMAGE_TAG=main

# DStack service address (GPU scheduler service)
# Supports both HTTP and Unix socket connections:
# - HTTP: http://host.docker.internal:14520
# - Unix socket: unix:///opt/dstack/dstack-v05x/run/teepod.sock
DSTACK_BACKEND_DSTACK_URL=http://host.docker.internal:14520

# ===== Worker Registration Configuration (Required) =====
# Note: Successful registration is required to communicate with the message network

# Registry center address (required)
REGISTRY_URL=http://localhost:9200

# Ethereum owner address (required)
OWNER_ADDRESS=0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb

# Node type (optional)
NODE_TYPE=node-H100x1

# ===== Dephy Worker Configuration (Message Network Component) =====
DEPHY_WORKER_IMAGE_TAG=v2.1.0
DEPHY_WORKER_REGISTRY=http://host.docker.internal:9000
